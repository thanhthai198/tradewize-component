import React from 'react';
import { type IImageDrawingCanvas } from './types';
declare const ImageDrawingCanvas: (props: IImageDrawingCanvas) => React.JSX.Element | null;
export default ImageDrawingCanvas;
