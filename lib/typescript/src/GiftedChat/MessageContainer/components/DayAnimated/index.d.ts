import { type DayAnimatedProps } from './types';
export * from './types';
declare const DayAnimated: ({ scrolledY, daysPositions, listHeight, renderDay, messages, isLoadingEarlier, ...rest }: DayAnimatedProps) => import("react/jsx-runtime").JSX.Element | null;
export default DayAnimated;
